{
    "sqltools.useNodeRuntime": true,
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "pytuf",
            "database": "${workspaceFolder:pytuf}/data/db.sqlite"
        }
    ]
}